apiVersion: v1
kind: Service
metadata:
  name: codepylot
  namespace: codepylot
  labels:
    app: codepylot
spec:
  type: ClusterIP
  selector:
    app: codepylot
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: {{ include "codepylot.fullname" . }}
  labels:
    {{- include "codepylot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codepylot.selectorLabels" . | nindent 4 }}
